
### Generated by sprinkler

general {
    gaps_in = 5
    gaps_out = 10
    border_size = @hydrate(border-size) 
    col.active_border = @hydrate(border-active-color) 
    col.inactive_border = @hydrate(border-inactive-color) 

    layout = master
}

decoration {
    rounding = @hydrate(border-radius) 
    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    blur {
        enabled = true
        size    = 8
    }
}
